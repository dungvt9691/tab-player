<% if @errors.blank? %>
$("#launch-modal").html("<%=j render @modal.gsub('-', '_'), tutorial: @tutorial %>")
$("#<%= @modal %>").modal({
  keyboard: false,
  backdrop: 'static'
})
<% else %>
showMessage("<%= render_value(@errors) %>", "error")
<% end %>